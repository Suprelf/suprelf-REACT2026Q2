import './topBar.css';

const TopBar = () => {
  return (
    <div className='bar-wrapper'>
      <button className='bar-button'>Controled Form</button>
      <button className='bar-button'>Uncontroled Form</button>
    </div>
  );
};

export default TopBar;
